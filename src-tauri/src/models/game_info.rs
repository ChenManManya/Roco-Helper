#[derive(Debug, Default)]
pub struct GameInfo {
    pub hex_id: String,
    pub hex_mapid: String,
    pub hex_channelid: String,
    pub hex_username: String,
    pub hex_position: String,
    pub uint_id: u32,
}